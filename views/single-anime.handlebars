{{!-- <section>
    <a href="#" class="flex flex-col items-center bg-white rounded-lg border shadow-md md:flex-row md:max-w-xl hover:bg-gray-100 dark:border-gray-700 dark:bg-gray-800 dark:hover:bg-gray-700">
        <img class="object-cover w-full h-96 rounded-t-lg md:h-auto md:w-48 md:rounded-none md:rounded-l-lg" src="{{posterImage.tiny}}" alt="anime poster image">
        <div class="flex flex-col justify-between p-4 leading-normal">
            {{#if titles.en}}
                <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{{titles.en}}</h5>
            {{else}}
                <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{{titles.en_jp}}</h5>
            {{/if}}
            <div class="mb-3 font-normal text-gray-700 dark:text-gray-400">{{synopsis}}</p>
            <p class="mb-3 font-normal text-gray-700 dark:text-gray-400">{{status}}</div>
        </div>
    </a>
   <a href="#" class="flex flex-col items-center bg-white rounded-lg border shadow-md md:flex-row md:max-w-xl hover:bg-gray-100 dark:border-gray-700 dark:bg-gray-800 dark:hover:bg-gray-700">
    <img class="object-cover w-full h-96 rounded-t-lg md:h-auto md:w-48 md:rounded-none md:rounded-l-lg" src="{{posterImage.tiny}}" alt="">
    <div class="flex flex-col justify-between p-4 leading-normal">
        <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{{titles.en}}</h5>
        <p class="mb-3 font-normal text-gray-700 dark:text-gray-400">{{synopsis}}</p>
    </div>

    


    <h2>Write A Review</h2>

    <form class="new-review-form">
        <div>
            <label for="review-title"> title </label>
            <input type="text" id="review-title" name="review-title"/>
        </div>
        <div>
            <label for="review-text"></label>
            <textarea id="review-text" name="review-text"></textarea>
        </div>
        <button type="submit" class="new-rev-btn">Post Review</button>
    </form>
</section> --}}

<div class=" m-20 justify-items-center grid">
    <div class="grid-flow-col">
        <img class="grid-poster" src="{{posterImage.tiny}}" id="animeHomePosterImage">
        <div class="px-6 py-4">
            {{#if titles.en}}
            <div class="font-bold text-m mb-2 overflow-ellipsis" id="animeHomeTitle">{{titles.en}}</div>
            {{else}}
            <div class="font-bold text-m mb-2  overflow-ellipsis" id="animeHomeTitle">{{titles.en_jp}}</div>
            {{/if}}
        </div>
    </div>
</div>

{{!-- <script src="/public/js/addReviewData.js"></script> --}}